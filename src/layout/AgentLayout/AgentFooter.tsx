import React, { FC } from 'react';
import { Layout } from 'antd';

const { Footer } = Layout;

export const AgentFooter: FC = () => {
  return <Footer style={{ backgroundColor: 'red' }}>hello</Footer>;
};
